# NoPE (No Positional Embeddings) configuration
# Inherits from base.yaml

defaults:
  - base

# PE configuration
pe_type: "nope"

# Experiment naming
wandb:
  tags: ["nope", "baseline"]
  name: "nope-smollm360m"

training:
  output_dir: "./outputs/nope"
